name: Publish node package
run-name: Publish node package by @${{ github.actor }}

on:
  workflow_call:
    inputs:
      working-directory:
        type: string
        required: true

jobs:
  publish:
    name: 'Bump version and Publish package'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: 'main-test'

      - name: 'Bump version'
        uses: phips28/gh-action-bump-version@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PACKAGEJSON_DIR: ${{ inputs.working-directory }}
        with:
          tag-prefix:  'v'
          target-branch: 'main-test' #TODO change
          major-wording: '#major'
          minor-wording: '#minor'
          patch-wording: '#patch'
          check-last-commit-only: 'true'
          default: prerelease #TODO remove
          skip-tag: 'true' #TODO remove

      - name: 'Setup node'
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'
          registry-url: https://npm.pkg.github.com/
          scope: '@mattilsynet'
          cache: 'npm'
          cache-dependency-path: |
            mt-kit/core/svelte/package-lock.json
            mt-kit/core/react/package-lock.json
            mt-kit/core/css/package-lock.json

      - name: 'Publish to GitHub registry'
        run: |
          git pull
          git config --global user.email "${{ github.actor }}@users.noreply.github.com" && git config --global user.name "${{ github.actor }}"
          npm install
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
